{% extends 'base.html.twig' %}

{% block title %}Schulklasse Bearbeiten{% endblock %}


    {% block stylesheets %}
        <link href="{{ asset('style/stylingLandingPage.css') }}" type="text/css" rel="stylesheet"/>
        <link href="{{ asset('/style/stylingSchoolClass.css') }}" type="text/css" rel="stylesheet"/>
    {% endblock %}

{% block body %}
    {% include "includes/_navbar.html.twig" %}

    <div class="text-4xl">
        <h1>Schulklasse Anlegen</h1>
    </div>

    <div class="content-form">
        {{ include('school_class/_form.html.twig') }}
        <div class="btn-links-under-form">
            <button class="save-update-btn-form">{{ button_label|default('Anlegen') }}</button>
            <div class="back-to-list-btn">
                <a class="anchor-back-to-list" href="{{ path('app_school_class_index') }}">Zurück zur Liste</a>
            </div>
        </div>
    </div>

{% endblock %}
