<form method="post" action="{{ path('app_school_class_delete', {'id': school_class.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ school_class.id) }}">
    <button class="create-new">Löschen</button>
</form>
