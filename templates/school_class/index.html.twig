{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Schulklasse Übersicht{% endblock %}

{% block stylesheets %}
    <link href="{{ asset('style/stylingLandingPageAndNavbar.css') }}" type="text/css" rel="stylesheet"/>
    <link href="{{ asset('/style/stylingSchoolClassAndDepartment.css') }}" type="text/css" rel="stylesheet"/>
{% endblock %}

{% block body %}
    <div class="text-4xl">
        <h1>Schulklassen</h1>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>
                    <form id="yearForm" method="get" action="{{ path('app_school_class_index') }}">
                        <select id="yearFilter" name="year" class="year-filter">
                            {% for year in allYears %}
                                {% if year == currentYear %}
                                    <option value="{{ year }}" selected>{{ year }}</option>
                                {% else %}
                                    <option value="{{ year }}">{{ year }}</option>
                                {% endif %}
                            {% endfor %}
                        </select>
                    </form>
                </th>
                <th>Schulstufe</th>
                <th>Schüleranzahl</th>
                <th>Repetentenanzahl</th>
                <th>Benutztes Budget</th>
                <th>Gesamtes Budget</th>
                <th>Aktionen</th>
            </tr>
        </thead>

        <script>
            document.getElementById('yearFilter').addEventListener('change', function () {
                document.getElementById('yearForm').submit();
            });
        </script>

        <tbody>
        {% for school_class in school_classes %}
            <tr>
                <td>{{ school_class.name }}</td>
                <td>{{ school_class.year }}</td>
                <td>{{ school_class.grade }}</td>
                <td>{{ school_class.studentsAmount }}</td>
                <td>{{ school_class.repAmount }}</td>
                <td>{{ school_class.usedBudget }}</td>
                <td>{{ school_class.budget }}</td>
                <td>
                    <div class="actionIcons">
                        <a href="{{ path('app_school_class_show', {'id': school_class.id}) }}"><img class="img-size" src="{{ asset('images/show.png') }}" alt="Anzeigen"></a>
                        <a href="{{ path('app_school_class_edit', {'id': school_class.id}) }}"><img class="img-size" src="{{ asset('images/edit.png') }}" alt="Bearbeiten"></a>
                        <a href="{{ path('app_school_class_duplicate', {'id': school_class.id}) }}"><img class="img-size" src="{{ asset('images/duplicate.png') }}" alt="Duplizieren"></a>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">Keine Einträge gefunden!</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="create-new" href="{{ path('app_school_class_new') }}">Neu Anlegen</a>
{% endblock %}