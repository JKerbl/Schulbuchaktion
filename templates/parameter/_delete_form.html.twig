<form method="post" action="{{ path('app_parameter_delete', {'id': parameter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parameter.id) }}">
    <button class="btn">Delete</button>
</form>
