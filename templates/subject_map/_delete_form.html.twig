<form method="post" action="{{ path('app_import_subject_map_delete', {'id': import_subject_map.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ import_subject_map.id) }}">
    <button class="btn">Delete</button>
</form>
